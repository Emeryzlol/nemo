Setup procedure (Python + OpenAI API)

1. Create python venv (3.13) and source into it
`source .venv/bin/activate

2. Export OpenAI key
`export OPENAI_API_KEY=sk....

3. Run test.py
`python nemo/test.py

4. Edit the message in test.py as needed